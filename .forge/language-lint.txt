# forge plain-language avoid-list — one word per line, matched whole-word,
# case-insensitive. The /forge:doctor plain-language check flags these in docs.
# Human-editable: add or remove a word here; do not touch the checker.
# Reference (not copied): ASD-STE100 Issue 9 — https://www.asd-ste100.org
# Borderline technical names (canonical, conformance, projection) are omitted
# on purpose — see docs/language.md.
idempotent
leverage
utilize
facilitate
instantiate
deterministic
orchestrate
granular
robust
seamless
performant
